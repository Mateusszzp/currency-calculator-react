import "./style.css"



const Button = (props) => (
    <p>
        <button 
            className="form__button"
             >
            Przelicz
        </button>
    </p>
);

export default Button;